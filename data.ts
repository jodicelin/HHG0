import { PortfolioItem } from './types';

// We now fetch data asynchronously in App.tsx to avoid module resolution issues
// with JSON files in certain browser environments.
export const MOCK_DATA: PortfolioItem[] = [];